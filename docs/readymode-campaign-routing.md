# ReadyMode campaign routing

Ready Ops resolves the current ReadyMode campaign name through `/readymode` and `resolve_readymode_campaign`, so the universal ReadyMode script no longer requires a `ReadyOpsSlug` campaign variable.
